<%= form_with(model: video_channel, local: true) do |form| %>
  <% if video_channel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(video_channel.errors.count, "error") %> prohibited this video_channel from being saved:</h2>

      <ul>
        <% video_channel.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
